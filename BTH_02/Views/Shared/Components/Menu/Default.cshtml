@model IEnumerable<BTH_02.Models.MenuItem>

<nav class="leftmenu" aria-label="Primary">
    <ul class="menu-list">
        @foreach(var item in Model)
        {
            <li class="menu-item"><a href="@item.Link">@item.Name</a></li>
        }
    </ul>
</nav>
