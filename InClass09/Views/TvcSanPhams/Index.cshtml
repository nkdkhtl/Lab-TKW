@model IEnumerable<InClass08.Models.TvcSanPham>

@{
    ViewData["Title"] = "Products";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">Products</h2>
        <a asp-action="Create" class="btn btn-success"><i class="bi bi-plus-lg me-1"></i> Create New</a>
    </div>

    <div class="card">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>@Html.DisplayNameFor(model => model.tvcMaSanPham)</th>
                            <th>@Html.DisplayNameFor(model => model.tvcTenSanPham)</th>
                            <th>@Html.DisplayNameFor(model => model.tvcHinhAnh)</th>
                            <th>@Html.DisplayNameFor(model => model.tvcSoLuong)</th>
                            <th>@Html.DisplayNameFor(model => model.tvcDonGia)</th>
                            <th>@Html.DisplayNameFor(model => model.TvcLoaiSanPham)</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
    @foreach (var item in Model) {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.tvcMaSanPham)</td>
                                <td>@Html.DisplayFor(modelItem => item.tvcTenSanPham)</td>
                                <td>@Html.DisplayFor(modelItem => item.tvcHinhAnh)</td>
                                <td>@Html.DisplayFor(modelItem => item.tvcSoLuong)</td>
                                <td>@Html.DisplayFor(modelItem => item.tvcDonGia)</td>
                                <td>@Html.DisplayFor(modelItem => item.TvcLoaiSanPham.tvcTenLoai)</td>
                                <td class="text-end">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary me-1"><i class="bi bi-pencil-fill"></i></a>
                                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info text-white me-1"><i class="bi bi-eye-fill"></i></a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger"><i class="bi bi-trash-fill"></i></a>
                                </td>
                            </tr>
    }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
